// Forca pt-BR e desativa a traducao automatica do navegador (deve vir primeiro).
import './src/web/locale';
import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
